services:
  - type: web
    name: songbook-api
    runtime: go
    rootDir: backend
    buildCommand: go build -o app ./cmd/api
    startCommand: ./app
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: FRONTEND_URL
        sync: false
